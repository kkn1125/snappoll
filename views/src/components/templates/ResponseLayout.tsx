import { SnapResponse } from '@models/SnapResponse';

interface ResponseLayoutProps {
  response: SnapResponse;
}
const ResponseLayout: React.FC<ResponseLayoutProps> = ({ response }) => {
  return <div>
    
  </div>;
};

export default ResponseLayout;
